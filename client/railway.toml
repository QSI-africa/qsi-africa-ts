[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[nixpacks.phases.setup]
nixPkgs = ["nodejs-22_x"]

[deploy]
startCommand = "npx serve -s dist -l 3000"

[env]
NODE_ENV = "production"
