# Server Configuration
PORT=3001

# Database Connection
DATABASE_URL=""

# OpenAI API Key
OPENAI_API_KEY=""

# Email Service (Nodemailer)
EMAIL_HOST=""
EMAIL_PORT=""
EMAIL_USER=""
EMAIL_PASS=""

# WebRTC relay for calls across different networks (required in production)
# Include both UDP and TLS relay URLs supplied by your TURN provider.
TURN_URLS="turn:turn.example.com:3478?transport=udp,turns:turn.example.com:5349?transport=tcp"

# Recommended: coturn REST API shared secret for short-lived browser credentials.
TURN_SHARED_SECRET=""
TURN_TTL_SECONDS="3600"

# Static credentials are supported only as a fallback when no shared secret is set.
TURN_USERNAME=""
TURN_CREDENTIAL=""
