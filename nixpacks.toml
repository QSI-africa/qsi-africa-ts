[phases.setup]
nixPkgs = ['nodejs_18', 'openssl']

[phases.install]
cmds = [
  'npm install',
  'cd server && npm install'
]

[phases.build]
cmds = [
  'cd server && npm run build'
]

[start]
cmd = 'cd server && npm start'
